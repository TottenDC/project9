import React from 'react';

const Home = () => {
    return (
        <div className="photo-container">
            <h3>Search through the latest flickr photos</h3>
            <p>Either select from one of the three pre-set buttons or search for your own inspiration. Enjoy!</p>
        </div>    
    );
};

export default Home;